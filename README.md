# software-engneering-team6

神戸高専 ソフトウェア工学 チーム6 のリポジトリです。
成果物として **ToDo 管理アプリ**（HTML / CSS / JavaScript）を開発しています。

## ディレクトリ構成

| ディレクトリ | 内容 |
| --- | --- |
| `sourceCode/` | ToDo アプリ本体のソースコード |
| `reports/` | 要件定義書などの提出書類 |

## アプリ概要

ブラウザだけで動作するクライアントサイドの ToDo 管理アプリです。
フレームワーク・ビルドツール・パッケージマネージャは使用しておらず、データはすべてブラウザの `localStorage` に保存されます。

### 主な機能

- タスクの追加・編集・削除・完了切り替え
- 期限による自動セクション分け（期限切れ / 今日 / 明日 / 今週 / 来週 / それ以降 / 期限なし）
- タイトル・メモの検索
- 重要度（高・中・低）およびジャンルによる絞り込み
- ジャンルの追加・リネーム・削除（ジャンル管理モーダル）
- 完了済みタスクの表示切り替え
- ブラウザを再読み込みしてもタスクを保持

## 実行方法

ビルド不要です。以下のいずれかの方法で起動します。

```bash
# 1. ファイルを直接開く
open sourceCode/index.html
```

```bash
# 2. 静的サーバー経由で開く（file:// で localStorage が制限される場合）
cd sourceCode
python3 -m http.server
# → http://localhost:8000 をブラウザで開く
```

## ファイル構成（`sourceCode/`）

| ファイル | 役割 |
| --- | --- |
| `index.html` | 画面構成（ヘッダー・フィルター・タスク一覧・各モーダル） |
| `style.css` | スタイル定義 |
| `app.js` | アプリケーションロジック（IIFE で全体を包む） |

より詳しい仕様は [`sourceCode/README.md`](sourceCode/README.md) を参照してください。

## データ構造

`localStorage` には以下の 2 つのキーで保存されます。

| キー | 内容 |
| --- | --- |
| `todoTasks_v1` | タスク配列 `{ title, note, due, priority, genre, completed }` |
| `todoGenres_v1` | ジャンル配列 `{ id, name }` |

タスクの `genre` はジャンルの `id` を保持します。

## 開発の進め方

- `main` を統合ブランチとし、各自の作業ブランチから Pull Request を作成してマージします。
- テストフレームワークは導入していないため、変更後はブラウザ上で動作を手動確認してください。
